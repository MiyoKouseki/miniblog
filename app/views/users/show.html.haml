= provide(:title, @user.name)
%h1
  = gravatar_for @user
  = @user.name 

%p#notice= notice

%p
  %b Name:
  = @user.name
%p
  %b Profile:
  = @user.profile
%p
  %b Blog url:
  = @user.blog_url

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path
